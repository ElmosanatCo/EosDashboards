# Current Project State

**Last updated:** 2026-09-05

## Phase

Initial authentication and application-shell implementation.

## Completed

- The empty GitHub repository was cloned locally.
- The repository-based project-memory design was approved.
- The implementation plan for the memory structure was prepared.
- The canonical project-memory structure and root agent instructions were created and verified.
- Project-wide development, UI, security, data, testing, deployment, and operational standards were approved.
- The initial intranet authentication experience and the deferred external-access discovery boundary were approved.
- Material UI, RTL-first Persian presentation, theme behavior, the amber default palette with five further approved choices, and Vazirmatn were selected.
- The approved standards were consolidated into canonical English and formal Persian documents.
- The repository-level shared resource hierarchy was created.
- The first implementation slice was approved: separate backend/frontend foundations, pre-provisioned system administrator, local username/password sign-in, mandatory SMS OTP, session management, and the initial SPA shell.
- Company branding, footer version/date/time, and closable internal workspace tabs were approved.
- On phone-sized layouts, the approved EOS logo and company name move from the fixed header into the temporary navigation menu; desktop layouts keep the brand in the header. Shared brand rendering and focused responsive component coverage are in place.
- Every user-visible number now renders with Persian digits, including numbers embedded in usernames, revealed passwords, personnel/organizational identifiers, versions, masked contact values, dates, times, counts, and IP addresses. Form/API values and internal technical identifiers retain their required LTR/ASCII representation.
- The Sahel Farsi-Digits WOFF2 variant is loaded as the secondary typeface for numeric-bearing controls and values, while Vazirmatn remains the primary text typeface. The dashboard metric browser flow verifies both Persian glyphs and actual Sahel font loading.
- On 2026-09-04 the sign-in brand uses the approved transparent EOS logo only in the desktop advertising panel, while mobile login shows the transparent logo above the form and aligned to the right. The authenticated user menu has a structured profile header, spaced icon/text actions, and secondary action descriptions; Google sign-in uses a local multicolor G mark; the header search has a left-side `Ctrl+K` hint; and icon-only/contextual controls have Persian hover hints. Browser verification covers the rendered desktop flow and the existing responsive shell.
- A follow-up browser correction keeps the search input's Persian-number font limited to the editable value, so its `Ctrl+K` helper remains in the primary Vazirmatn typeface. Audit filters now use a responsive grid with bounded controls; at narrow widths they wrap inside the filter panel instead of extending the page horizontally. Focused component tests and mocked authenticated browser checks cover both regressions.
- The initial authentication and tabbed-shell design was reviewed and approved.
- A task-by-task, test-driven implementation plan was prepared.
- The user selected inline plan execution with review checkpoints.
- .NET SDK 10.0.400 and Node.js 24.19.0 were installed and verified.
- The isolated `feature/initial-authentication-shell` worktree was created.
- The independently buildable backend solution and React frontend foundation were scaffolded and verified.
- The Domain authentication, session, role, preference, and audit state model was implemented and verified.
- The Application authentication contracts and use cases were implemented and verified with correlation-aware audit attribution, non-cancelable OTP security commits, and access tokens capped by absolute session expiry.
- EF Core SQL Server persistence, explicit repositories, guarded database integration tests, the initial identity migration, and its reviewed idempotent deployment script were implemented and review-hardened with tracked provisioning mutations plus live uniqueness and rowversion-concurrency coverage.
- Infrastructure authentication security primitives were implemented, review-hardened, and verified: secure OTP/opaque-token generation, keyed HMAC-SHA256 hashing, restart-persistent purpose-isolated Data Protection mobile encryption, strict JWT issuer/audience/signature/HS256/lifetime validation with explicit expiry, typed startup-validated security options, and dependency injection. Pending a formal mask-format decision, the implementation conservatively exposes only the final four mobile digits (`*******6789`).
- The replaceable company SOAP SMS adapter was implemented and verified: typed startup-validated HTTPS endpoint/timeout options, one named `HttpClient`, SOAP 1.1 request creation with XML-safe message/mobile serialization, a configured deadline across headers and asynchronously read body, a fully buffered 64 KiB response limit before DTD-prohibited EOF-complete parsing with exactly one result, safe failure mapping, caller-cancellation propagation, and no automatic retry.
- The deployment-only System Administrator provisioner was implemented and verified: normalized idempotent user/role creation and profile updates, exact role-code validation, protected mobile storage with masked-only output, cross-process serialized transactional generated-ID/role/audit persistence, safe interactive confirmation, and composition independent of SMS configuration.
- The user required a cost-conscious development workflow: focused essential tests during implementation, broad verification only at meaningful checkpoints, and no subagents or repeated independent review loops without explicit approval or exceptional documented risk.
- The user explicitly selected Inline Execution as the default project workflow and asked that Subagent-Driven execution not be suggested in future unless explicitly requested.
- The user explicitly approved tracking local development server settings, including SQL credentials, service endpoints, and API security keys, in this private repository's API/IIS configuration. Public frontend endpoint settings may also be tracked; server credentials and private keys remain server-side because frontend build values are browser-visible. Use the established `D:\Workspaces\ChatGpt\Private Data For AI Projects\EosDashboards` directory as a fallback without repeatedly requesting its values. Values remain excluded from documentation and tool output; production values and personal data remain outside source control.
- The available SQL Server database and IIS sites are development-only resources on the user's machine. Task 13 targets them for local deployment and smoke testing; production deployment to company servers remains a later, separate activity.
- Secure API endpoints expose local username/password challenge creation, purpose-isolated OTP verification, password recovery and signed-in password change, JWT-protected current-user access, refresh rotation, logout, current-user preferences, safe problem details, rate limiting, exact-origin CORS, OpenAPI, and live/ready health checks.
- The React SPA has a locally hosted Vazirmatn font, Persian RTL Material UI theme, polished local-credential sign-in, recovery and password-change views, in-memory access-token client, fixed application shell, home tab, serializable closable-tab workspace, Persian status clock, and server-synchronized appearance/sidebar preferences.
- A 2026-09-03 UI correction replaced the raw session-check text with a branded loading state, removed the forced light sign-in theme, corrected dark-mode contrast tokens and primary-button hover text, made sidebar closure functional, and moved the status bar to the full application width. A repeated development sign-in failure was traced to Vite returning its own 404 for `/api/*` because it had no API proxy; the generic credential/SMS error then concealed that routing defect. Vite now proxies `/api/*` to the local IIS API while presenting the approved local UI origin. A browser regression test requires the API's `refresh_rejected` response rather than Vite's 404, so development execution cannot silently lose its API route again. Regression coverage also verifies the loading state, saved dark appearance, full-width status bar, sidebar interactions, and primary-button hover contrast with a simulated credential/OTP flow.
- A follow-up rendered-UI correction on 2026-09-03 found that a failed preference write rolled back the user's theme and sidebar choice, making both controls appear broken. Local choices now remain applied and are retained per user even when server synchronization fails, without interrupting the workspace with a repeated user-visible notice. RTL Drawer placement now opens beside the right-side menu control, internal workspace tabs remain neutral and unbranded, the browser tab title is `داشبوردهای علم و صنعت`, and the home content fills and aligns with the workspace at every width. A deployed-IIS browser check also found that the workspace tab provider dropped the `/EosDashboards/` application base from the browser URL; it now preserves that base for all tab URLs. Component and mocked-network browser tests cover the fallback, title, alignment, Drawer edge, and IIS base-path behavior.
- The approved UI quality correction on 2026-09-03 made the first appearance-toggle click compare against the resolved system appearance, so it always changes the rendered theme. It added six persisted palettes (forest green, navy, turquoise, plum, amber, and burgundy), with amber as the default and a deep restrained brown in dark mode. On phones the navigation is now a temporary overlay and never reduces the workspace width; its modal starts below the fixed header and ends above the status bar, has an in-panel close action, and can also be dismissed by touching outside it. OTP phone numbers and password entry are explicitly left-to-right inside the Persian RTL interface, and the recovery action has deliberate spacing. Focused component tests, backend preference validation, mocked authenticated-browser tests, and desktop/mobile browser checks cover these behaviors.
- Amber became the default palette on 2026-09-03 for users without an existing server or local palette preference. Existing saved palette choices are preserved. IIS release `20260903-115548` serves this UI artifact successfully over HTTPS.
- A 2026-09-03 browser investigation confirmed that an HTTP Vite preview cannot be used to validate the secure `__Host-` refresh-session lifecycle. Manual local credential, OTP, refresh, logout, and server-preference validation must use the installed `https://localhost/EosDashboards/` IIS application; the Vite preview remains suitable for frontend development and mocked browser tests. The refresh cookie policy remains Secure and is not weakened for development.
- Fresh pre-integration verification passed on 2026-09-03: the Release backend suite completed 172 tests, including 102 SQL-backed integration tests; frontend lint, typecheck, formatting, build, 10 component/unit tests, and one mocked-network browser flow completed successfully. Separate API/UI publish artifact inspection previously found no source maps or detected embedded secrets.
- The approved manager-facing workforce-operations UI alignment was integrated into `main` on 2026-09-03 as merge commit `18ff610`: dark/teal is now the default; teal, indigo, emerald, amber, orange, and rose are the supported persisted accents; legacy stored palette values safely render as teal; the theme supplies the approved dark/light tokens, compact geometry, flat panels, short motion, and gold tab indicator. The existing persistent collapsible navigation behavior remains unchanged. The home tab is an honest operational no-data state; no dashboard data, workflows, or simulated status was introduced. Fresh post-merge verification passed: 200 Release backend tests, including 115 SQL-backed integration tests; frontend typecheck, formatting, production build, 25 component/unit tests, and 7 browser flows. Frontend lint completed with existing React warnings only; desktop and mobile browser checks confirmed the dark shell, palette toggle, tab, and mobile navigation without relevant console errors. It was published to local IIS release `20260903-171004` from `main` commit `15565b2`; API readiness, UI entry, and an internal SPA refresh route returned HTTPS HTTP 200, and a fresh browser visit showed the current sign-in screen with no console errors. No authenticated smoke flow was run because no user credential was supplied.
- A local browser-test-port collision was resolved without touching an unrelated Python service: Playwright now accepts `EOS_PLAYWRIGHT_PORT` and was verified on port 4174. This prevents the test suite from accidentally reusing content served by a process outside the project.
- Local development deployment artifacts were produced in a versioned temporary directory. Both API and UI contain `web.config`; the reviewed initial idempotent SQL migration script remains available.
- The initial authentication and application-shell implementation was merged into `main` and published to the configured GitHub remote on 2026-09-03 as merge commit `d51fe35`. The external SMS connectivity blocker remains explicit and does not invalidate the user-confirmed live username/password-and-OTP sign-in completed on 2026-09-02.
- The approved EOS SVG was received on 2026-09-02, stored under `resources/branding/eos.svg` without modification, and verified against its SHA-256 record.
- The local IIS deployment uses separate `Default Web Site` applications: UI `/EosDashboards` in `EosDashboardsUiPool` and API `/EosDashboardsApi` in `EosDashboardsApiPool`, both under `C:\inetpub\wwwroot\EosDashboards\` with immutable versioned releases. Both pools use No Managed Code; the current local UI and API readiness endpoints return HTTPS HTTP 200.
- IIS URL Rewrite is not installed locally. The UI SPA fallback uses IIS custom 404 execution, avoiding that unavailable module. API Anonymous Authentication is enabled and Windows Authentication is disabled. The API also disables IIS automatic authentication in-process so the retired Negotiate handler cannot be injected during local credential sign-in.
- The development SQL connection, SMS endpoint, authentication keys, local IIS origin, and API-only key-ring path are tracked in the API development configuration under decision 0006. The persistent key ring remains outside the web root. The deprecated private-data helper is limited to explicitly requested first-administrator provisioning or repair. The local development migration completed and one initial System Administrator user, role assignment, and provisioning audit record were created. No personal or production value is stored in this repository.
- The authentication documentation was aligned on 2026-09-03: the only phase-1 sign-in flow is pre-provisioned username/password followed by mandatory SMS OTP. Legacy integrated-directory sign-in wording was removed from current specifications, decisions, and operations guidance.
- A follow-up administration UI correction on 2026-09-03 resolved three rendered defects: the create-user dialog spinner was caused by treating React Query's intentionally disabled user-details query as pending; the create/edit form surfaces now fill their dialog width; and every manager-facing accent panel uses the shared `eos-accent-card` hover rule to turn its top line gold. Red/green component tests cover the create form, dialog width, and theme rule. Release `20260903-231951` was built from committed source `46326ea` and verified in Chrome at desktop and phone sizes: the create-user and create-department dialogs rendered their fields, desktop form/card gaps were limited to the surrounding 1px border, and a hovered dashboard card computed the gold `#E0A13A` top line. API readiness, UI entry, and a refreshed department route returned HTTPS HTTP 200; no relevant browser-console messages were present.
- The redundant user-account-name field was removed on 2026-09-03 after confirming that local sign-in uses the username and Google sign-in uses its linked identity. The field is gone from user forms, administration request/response contracts, authenticated-user responses, the domain model, and the deployment provisioner; migration `20260903200248_RemoveUserAccountName` drops the legacy SQL column and index. A verified backup was taken and this migration was applied to the local development database, with the column absence and migration-history entry confirmed. IIS release `20260903-234405` was then published from the matching API/UI artifacts; API liveness/readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200.
- A 2026-09-04 rendered-UI correction replaced the audit custom-range year/month/day selects with a single Persian-calendar month view and a separate time field. Manager-facing page frames and the shell content gutter now use the full available workspace width with a consistent 16px edge gutter, removing excessive desktop side whitespace while preserving mobile alignment. Focused component tests, the full frontend component suite, typecheck, and mocked authenticated desktop/mobile browser checks passed.
- On 2026-09-04 the audit filters were made self-describing: event filtering now uses Persian event labels, while actor and subject filtering use user-name/personnel-code selectors and submit the internal user ID behind the scenes. This prevents administrators from having to guess opaque database IDs; system-generated events remain visible when the actor filter is left at «همه انجام‌دهندگان».
- The audit workspace now keeps its title/filter panel and refresh action fixed within the page frame; only the audit table panel scrolls vertically, so long histories do not move the controls out of view. A mocked authenticated browser assertion covers the bounded scroll region.
- The user confirmed on 2026-09-04 that this is a whole-application scroll rule: the shell header, internal tabs, and status bar remain fixed, the central workspace is the only page-level scroll area, and data-heavy pages may add a bounded inner data-panel scroll without moving their controls.
- IIS release `20260904-002509` was published from the current `main` source (`aa25b62`) after paired API/UI artifact inspection. API liveness/readiness, UI entry, and an internal SPA refresh route each returned HTTPS HTTP 200; the deployed UI artifact was also checked for the Persian audit-filter labels.
- IIS release `20260904-004357` was published from committed source `e71d431` with the mobile brand relocation. API liveness/readiness, UI entry, and an internal SPA refresh route returned HTTPS HTTP 200; the served UI was checked for its IIS base path, title, EOS asset, and company name. An authenticated IIS smoke flow remains unrecorded because no credential was supplied.
- IIS release `20260904-081343` was published from committed source `b6c2cd0` with shared Persian user-facing number formatting. API liveness/readiness, UI entry, and an internal SPA refresh route returned HTTPS HTTP 200; the served UI was checked for its IIS base path, title, EOS asset, company name, and Persian-number logic. An authenticated IIS smoke flow remains unrecorded because no credential was supplied.
- On 2026-09-04 the Home tab became the role-aware workspace hub: it presents guidance and the authenticated user's name and department, derives authorized `امکانات در اختیار شما` and `کارهایی که می‌توانید انجام دهید` content from the existing role-filtered workspace targets, shows an honest `هشدارها و کارهای نیازمند اقدام` empty state until an approved real alert/action data source exists, provides `ادامهٔ کار` for open workspace tabs, and reserves `امکانات آینده` for approved capabilities as they are added. Home must not fabricate alerts, metrics, tasks, or future capabilities, and it must not reveal unauthorized pages.
- Commit `2c11c40` merged the role-aware Home workspace hub into `main`. Final verification completed with 27 frontend test files/63 tests, 9 mocked browser-flow tests, lint, formatting, and a production UI build. Release `20260904-103208` was published to local IIS from the merged source; API liveness/readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200, and the served bundle contained the Home workspace sections.

## In progress

- The final manager-slice UI pass on 2026-09-04 made the shared help guide for the task catalog reflect the actual controls: target department, search and pagination, task/project type, per-row required-skill count, edit/deactivation actions, and the public/department skill-selection dialog with its save flow and limitations. The home workspace now also has metadata for both catalog routes. Focused and full frontend verification pass 32 files and 75 tests.
- The catalog mutation correction is implemented: duplicate skill/task create and rename attempts now return stable conflict codes and visible Persian feedback while preserving entered values; catalog reads expose lifecycle status, and manager/HR catalog surfaces provide active/inactive/all filtering with authorized reactivation for soft-deactivated skills and tasks. Reactivation now requires the shared Persian confirmation dialog, and cancellation sends no mutation request. The full frontend suite (32 files, 78 tests), frontend typecheck, focused formatting, and Release API/UI builds pass. The current environment did not expose the isolated SQL-test connection for this follow-up, so no new SQL-backed run was attempted.
- The catalog reactivation safeguard is now covered by an explicit Persian confirmation in the department catalog and Human Resources public-skill dialog. Cancellation leaves the inactive item unchanged and sends no activation request; confirmation performs the authorized activation.
- The catalog mutation correction was committed as `3766c41` and published from matching Release API/UI artifacts to local IIS release `20260904-204005`. The publisher's migration preflight passed, API live/ready, UI entry, and SPA refresh checks returned HTTPS HTTP 200, the served UI bundle contains the inactive-catalog query path, and the authenticated catalog page now visibly shows the وضعیت filter. No schema migration was required.
- The reactivation-confirmation correction was committed as `e0b7416` and published from matching Release API/UI artifacts to local IIS release `20260904-204655`. The artifact inspection confirmed the IIS base paths, inactive-catalog query path, and activation-confirmation text; API live/ready, UI entry, and SPA refresh checks returned HTTPS HTTP 200, and the authenticated catalog page visibly shows the وضعیت filter from the new release. No schema migration was required.
- A production deployment requirement is now explicit: because the developer will not have direct access to the company database, each API release with pending EF migrations must run its reviewed migration script or bundle through a separately authorized deployment identity before IIS activation. Runtime API startup must remain migration-free; the production CI/CD or server-side release mechanism is not selected or implemented yet. The Persian operator guide is `docs/operations/production-database-migration-fa.md`.
- A Persian production installation runbook was added for a multi-site IIS server: it covers collision-safe bindings, dedicated UI/API application pools, versioned artifacts, production configuration, backup and migration ordering, smoke tests, and rollback. The actual company hostname, certificate, paths, identities, Secret Store, and operational owner remain environment decisions for the infrastructure team.
- Pre-publication verification on 2026-09-04 passed the full Release backend suite with the isolated `EosDashboard_IntegrationTests` catalog: 262 tests passed across Domain, Architecture, Application, and SQL-backed Integration suites. The first suite attempt correctly failed fast at the environment boundary because `ConnectionStrings__EosDashboardTests` was absent; the reusable remedy is recorded in `standards.md`. The configured primary local database was backed up to the approved local backup directory and verified, then `dotnet ef database update` ran with `ASPNETCORE_ENVIRONMENT=Development` and `--configuration Release`; no migrations were pending through `20260904135025_RepairPersistedPersianJobDescriptionDates`.
- The integrated source was merged into `main` as `0eab5bb` and the API/UI Release artifacts were built from that exact commit. The first IIS publication attempt stopped at `administrator-check` because the invoking PowerShell session was not elevated; no IIS application path was switched. Rerun `scripts/Publish-LocalIisRelease.ps1` from an elevated PowerShell window with the inspected versioned artifacts after confirming the latest migration preflight.
- A follow-up IIS publish attempt was run from `C:\Windows\system32`, so PowerShell resolved the relative `-File` and artifact paths outside the repository and stopped before IIS preflight. The durable remedy is to change directory to the repository root or pass absolute paths; the migration identifier must be supplied without an escape character before `_`. No IIS application path was switched.
- Local IIS publication then completed successfully as release `20260904-192500` from the inspected API/UI artifacts built from `0eab5bb`. The publisher confirmed the latest migration preflight before switching the versioned API and UI paths; post-publication HTTPS checks for API liveness, API readiness, UI entry, and the SPA refresh probe each returned HTTP 200.
- On 2026-09-04 the approved department-manager job-description foundation was implemented on the current feature branch: database-backed job-description records and versions, manager scope over direct child departments, workflow and quality statuses, department/task/skill catalogs, deterministic skill/task analysis primitives, dashboard metrics, HR review endpoints, role-aware pages, manual draft creation, multi-file `.xlsx` import with per-file results, generated standard Excel artifacts stored with the database version, secure scoped download, and revision-as-new-version behavior. A healthy/incomplete list calculation defect was corrected by loading the related tasks and skills before evaluating quality. Focused domain/application/integration tests and the full SQL-backed integration suite pass; frontend tests, typecheck, and formatting pass.
- The manager catalog surface was refined on 2026-09-04: skills and tasks now have separate authorized workspace pages with searchable, paginated tables suitable for large catalogs. Task rows expose required-skill maintenance in a focused dialog, and department selectors show department names without principal/child annotations. The job-description page remains focused on personnel records and no longer embeds catalog management.
- The combined department selector rule was aligned across the manager dashboard, job-description management, and both catalog pages on 2026-09-04: `همه بخش‌ها` is the first selectable option and is represented by an explicit all-value so the combined view is loaded reliably; named department selections continue to scope data to that department.
- Public skill management was extended on 2026-09-04: the skill catalog has separate عمومی/اختصاصی tabs, the all-departments selector loads every managed department, and specific-skill domains include the department name. Public skills retain their registering department as ownership metadata; managers can register and manage them while usage remains local, and cross-department usage transfers edit/delete authority to Human Resources. The ownership column was added by migration `20260904120345_PublicSkillOwnership` and applied to the local development database.
- The Excel importer was corrected on 2026-09-04 after tracing the generic invalid-data result to profile labels and values stored in one cell (for example, `label: value`) in the supplied workbooks. The parser now supports inline labels, common Persian label variants, and the existing tolerant missing-field rules. The focused adapter regression test passes, and all 17 `.xlsx` files found in the local `D:\TMP` folder were read successfully without database writes.
- The approved unresolved-value resolution flow was implemented on 2026-09-04: unmatched imported skill names and task rows are retained in version-owned SQL tables, appear in detail and analysis, and produce `ناقص` plus `منتظر رفع نقص`; manager approval returns the stable `incomplete_job_description` conflict until the manager resolves every raw value and required field. Revision creates a new immutable version, with public/target-department skill choices and authorized create-new skill/task controls; a newly created task can be marked `پروژه`. The generated Excel artifact retains unresolved text and task order. Migration `20260904124328_PreserveUnresolvedJobDescriptionInputs` was applied to the local development database.
- The manager job-description page now renders raw unresolved values in view/edit, keeps their descriptions and dates editable, offers catalog mapping or creation, disables submission while any unresolved value remains, and displays the new workflow status. Its target-department filter is above the personnel list; the create/edit form also carries the authorized target-department selector, and revisions can move the current record to another authorized department while retaining the prior version history. New skills and tasks use that form target, with a `عمومی` skill checkbox opting a skill out of department scope. Job-description task dates are date-only in the form and are persisted as date-only values. The department dashboard includes a `منتظر رفع نقص` follow-up count. The importer still reports informational catalog suggestions and does not silently create entries. The supplied personal sample workbooks were inspected read-only; the approved personnel workbook sample is represented in the repository only by the sanitized layout template `resources/templates/job-description-reference.xlsx`, with its sample values removed.
- The job-description edit experience was tightened on 2026-09-04 after the rendered form became difficult to scan: a persistent summary shows the total and category counts of incomplete items, profile/skill/task groups are collapsible, and only one task accordion is expanded at a time while each collapsed header retains its matching warning. System-generated sequence/count labels use Persian digits. The importer now converts Persian-calendar workbook dates to Gregorian `DateOnly` values with `PersianCalendar`; the focused adapter regression test covers `1402/02/11` with Persian digits in the workbook and passes. Selecting an existing task no longer hides `ایجاد وظیفه جدید`; the creation mutation receives explicit current-row values and reports a visible error when the catalog request fails. The inline catalog-task editor opened from a row now renders immediately below that row's matching combo, before the workload and description fields; the redundant end-of-list catalog-task button was removed. Personnel code is now required when the manager creates or revises a record, while imports without that Excel field remain editable as `ناقص`. A resolved imported skill now immediately highlights its catalog skill in the selectable skill list, and changing the mapping moves that highlight to the new skill; the focused page test covers both transitions.
- On 2026-09-04 each resolved job-description task now stores `متوسط ساعت کاری مورد نیاز در هفته` as a database-only decimal value from 0 to 168. Manager create/revise flows require it; imported workbooks remain tolerant because the value is not an Excel column and must be completed in the manager form. Migration `20260904134528_AddWeeklyHoursToJobDescriptionTasks` adds the field and repairs previously persisted Jalali dates that had been stored as Gregorian year 1402, using a temporary SQL Server conversion function. A direct database check then exposed that unresolved imported task rows use a separate table, so `20260904135025_RepairPersistedPersianJobDescriptionDates` extends the repair there; both matched and unresolved tables now have zero old-calendar-year rows. The local development database was updated successfully. The field is returned to manager and Human Resources detail views but is excluded from generated Excel artifacts.
- Global scrollbar styling now follows the active appearance and accent palette through the shared MUI baseline: themed tracks, accent thumbs, and stronger accent hover states apply across the shell and independently scrolling page regions.
- The catalog pages now bound their page frame to the workspace and let the table panel consume the remaining height, so a long skill/task list scrolls inside its table without creating a second page-level scrollbar. A focused layout regression test and an internal-browser measurement confirm the page scroll height equals its client height while the table remains independently scrollable.
- The management-list typography pass on 2026-09-04 aligned department, user, and skill/task catalog surfaces on the shared compact rhythm: Vazirmatn 14px text for Persian names, 12px table-cell vertical padding, and Sahel FD only for numeric-bearing user values. Full department/user form labels and department selectors no longer apply the numeric font to ordinary Persian names. The focused regression tests, full frontend suite (34 files, 82 tests), typecheck, formatting, production build, and mocked Playwright desktop/mobile render check pass; lint retains only the repository's existing warnings.
- The management-list typography correction was merged into `main` as `9336ec3`, pushed to `origin/main`, and published to local IIS release `20260904-231157` from the matching API/UI artifacts. The publisher confirmed the expected development migration, switched the separate API/UI application paths, and API readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200.
- On 2026-09-05 the shared page-help action moved from each page frame into the fixed header beside the user menu. It opens the Persian guide for the active workspace tab, preserving the four fixed sections وظایف این صفحه، امکانات، شیوه انجام کار، و محدودیت‌ها and eliminating overlap with management-page action buttons. Guide content remains centralized in `frontend/src/components/pageGuides.ts` and must be updated with each approved capability stage. Focused component coverage, the full frontend suite, typecheck, build, formatting, and the mocked browser flow covering users, departments, and job descriptions passed.
- On 2026-09-05 the header command search was returned to the primary Vazirmatn typeface at normal weight because the numeric-focused Sahel FD font made Persian search text appear too heavy. The status-bar clock now displays Persian-calendar local hours and minutes without seconds. Focused component regressions cover both visual-format rules.
- On 2026-09-05 the selectable theme accents gained a new `نارنجی` option with `#E65100` as its dark-mode base and swatch; the existing `کهربایی` option remains unchanged. The frontend palette resolver and backend preference allowlist both accept the new `orange` identifier, with focused frontend and application tests covering selection and persistence.
- On 2026-09-05 the department create/edit form was simplified by removing the redundant explanatory sentence about independent and child departments. The `واحد والد` selector and its existing parent-department behavior remain unchanged; the focused form regression verifies the text is absent.
- On 2026-09-05 the mobile navigation close control moved out of the extra toolbar row and into the physical upper-left corner of the temporary Drawer. The home row now starts directly after the mobile brand area; RTL-safe browser geometry coverage verifies the close control and the absence of the extra vertical gap.
- On 2026-09-05 the fixed-header global search placeholder was reduced to a compact 12px size while entered search text remains at the normal Vazirmatn size and weight. The placeholder rule uses a dedicated global CSS class because the RTL Emotion selector path rejects nested placeholder selectors; focused component coverage and the authenticated browser flow verify the rendered rule.
- On 2026-09-05 the department-manager job-description workflow gained explicit-confirmation archive for approved records and delete for only unapproved drafts (`منتظر تأیید`/`منتظر رفع نقص`); retained workflow history cannot be deleted through the manager surface. Standard Excel downloads are regenerated from the approved sanitized `resources/templates/job-description-reference.xlsx` layout based on the supplied personnel workbook sample, preserving its RTL sheet, merged cells, dimensions, styles, theme, and print setup while filling current values. Focused application, workbook-adapter, and page tests passed. The first broad backend run correctly stopped when `ConnectionStrings__EosDashboardTests` was absent; rerunning against the isolated `EosDashboard_IntegrationTests` catalog passed all 271 backend tests, and the full frontend suite passed 35 files/95 tests.
- A downloaded workbook defect was traced to duplicate task-row merge references left behind by the template transformation, an orphaned empty `sharedStrings.xml` copied from the sanitized template, the database-only personnel code being rendered beside the name, and the parser treating the visible task sequence as `ستون ۱` in the description. The generator now removes the obsolete shared-strings part and relationship, de-duplicates template merges, omits the personnel code, and the parser excludes the sequence column from free text. The focused adapter regression covers all four cases.
- The finalization artifact set `20260905-112142` was built from commit `ab3a7fc`, inspected for the IIS/API bases, archive/delete UI markers, API `web.config`, and absence of source maps, and then successfully published to local IIS from an elevated PowerShell session. The publisher reported `Local IIS release 20260905-112142 is ready`; the earlier non-elevated/UAC and Execution Policy failures were resolved with the documented Administrator preflight and process-scoped PowerShell bypass.
- On 2026-09-05 the shell refinement now uses the active theme's solid primary color across the full main header with contrast text and icons; the global search field, placeholder, shortcut badge, and outline inherit that readable header contrast. The active workspace tab's accent sits directly at its lower edge with an upward fade, and the active page workspace's upper-right-to-lower-left gradient remains limited to the first quarter. The active sidebar item now has a low-contrast accent wash and a narrow physical-right indicator; inactive tabs and menu items remain flat. Component and mocked Chrome coverage verify the header/search contrast, gradient directions, fade boundaries, and active-menu treatment.
- The active page workspace gradient now starts with a stronger theme-color presence (`0.16` opacity), eases to `0.05` by 14%, and still disappears at 25% so the content area remains readable.
- The shell-gradient preference is now stored per user through `UserPreferences.GradientsEnabled`, defaulting to enabled through migration `20260905065524_AddGradientPreference`. The user-menu color section exposes the Persian toggle `فعال‌سازی گرادیانت‌ها`; turning it off removes the active-workspace-tab and workspace-content gradients while leaving the main header solid and preserving the active sidebar-menu gradient. Component, provider, application, and mocked Chrome coverage verify persistence and both visual states.
- The gradient-preference release source was committed to `main` as `52ce28d`. Fresh verification passed the full frontend suite (35 files/93 tests), full Release backend suite (267 tests with the isolated SQL catalog), frontend typecheck, IIS build, lint, focused formatting, and the targeted authenticated browser flow. A verified local database backup was taken, migration `20260905065524_AddGradientPreference` was applied from the Release build, and elevated local IIS publication completed as release `20260905-103416` from matching API/UI artifacts. API live/ready, UI entry, and refreshed SPA route each returned HTTPS HTTP 200; the published UI artifact retained the IIS/API base paths and gradient toggle marker. The release commits were pushed to `origin/main`.
- The user established the Persian release shorthand `نهایی کن` for a completed change. It authorizes the verified commit/merge, push to the configured remote, matching local IIS publication, and post-publication smoke checks; it does not authorize discarding unrelated user changes or production deployment.
- Commit `68a0f98` contains the repaired job-description workbook export and the stronger first-quarter workspace gradient and has been pushed to `origin/main`. Release artifacts `output/iis-api-20260905-114119` and `frontend/dist` were published to local IIS as release `20260905-114119` from an elevated session with process-scoped Execution Policy bypass. Post-publication probes returned HTTP 200 for API live, API ready, UI entry, and an internal SPA refresh route.
- Local IIS release `20260905-100445` was published from commit `3f651b5` after the themed shell contrast refinement. The first publisher invocation correctly stopped at the non-elevated administrator check without switching IIS paths; the elevated rerun copied the matching API/UI artifacts and switched the release. API live/ready, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200. No credential-bearing live sign-in smoke was run because no live credential was supplied.
- Local IIS release `20260905-092515` was published on 2026-09-05 from committed source `64d86d9`, including the orange theme palette and the approved shell corrections. API liveness/readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200 after the switch; the API/UI artifacts were built from the same commit and inspected for the IIS base paths, application title, branding, API base, and `#E65100`. No credential-bearing live sign-in smoke was run because no live credential was supplied.
- Local IIS release `20260905-084301` was published on 2026-09-05 from committed `main` source `7a167f7`, containing the fixed header help behavior, normal-weight primary search typography, and minute-only status clock. The publisher's development-migration preflight passed; API liveness/readiness, UI entry, and the refreshed SPA route each returned HTTPS HTTP 200. No credential-bearing live sign-in smoke was run in this release because no live credential was supplied.
- A global destructive-action safeguard was added on 2026-09-04: delete, removal, and deactivation controls now use the shared Persian confirmation dialog. Cancellation never mutates local state or sends an API request; this covers catalog entries, organizational units, public skills, user deactivation, and unsaved task rows.
- A local browser smoke check on 2026-09-04 found the new dashboard and job-description requests reaching the API but failing because the development database had not received `20260904091824_DepartmentJobDescriptionFoundation`; the API logged `Invalid object name 'JobDescriptionVersions'`. The migration was applied to the configured development database, the API was restarted, and the authenticated dashboard now renders its database-backed metrics while the job-description list returns the honest empty state. The Vite development proxy also supports direct local API mode through `VITE_API_PROXY_TARGET=http://localhost:5171` while retaining the IIS-prefixed default.

- On 2026-09-05 the Human Resources slice was implemented and merged into `main` as merge commit `3f70136`: the role-protected organization dashboard now supports all-department or single-department selection, database-backed metric cards, per-department change statistics, and paged change history. The renamed `مدیریت شرح وظایف` workspace now has pending-review, approved-history, and public-skill tabs with view/download actions, rejection reasons, current-versus-immediately-previous comparison, and public-skill edit, soft-delete, reactivation, and audited merge. Public-skill merge transfers existing references to the explicitly named surviving skill and deactivates the source without deleting history. The Application suite (92 tests), protected API endpoint tests (6), frontend suite (36 files/97 tests), typecheck, lint, IIS build, and mocked browser suite (9 flows) passed. The merge is pushed to `origin/main`; local IIS publication and its smoke result remain pending an elevated PowerShell session.
- Finalization preflight on 2026-09-05 freshly passed the full frontend suite (36 files/97 tests), full Release backend suite (284 tests with the isolated SQL catalog), frontend lint, and typecheck. The repository-wide Prettier check still reports only the known unrelated baseline differences in `DepartmentManagementPage.test.tsx` and `UserManagementPage.test.tsx`; no release file was changed. Release artifact rebuild, local IIS publication, and post-publication smoke checks remain pending an elevated PowerShell session (`elevated=False`); no retry or UAC bypass was attempted.
- To remove the repeated elevation stop, commit `6b0d3d6` added `scripts/Finalize-LocalIisRelease.ps1`, which requests UAC with `Start-Process -Verb RunAs`, builds API/UI artifacts from one committed source, invokes the guarded publisher, and keeps the latest migration preflight. Its five Pester checks pass. The user then ran the canonical command from a truly elevated PowerShell; IIS release `20260905-143557` completed from `main` commit `e849fae`. The publisher's elevated application/pool check passed, both release directories exist, and API live/ready, UI entry, and SPA refresh probes each returned HTTPS HTTP 200. A separate read-only WebAdministration query from the non-elevated tool shell remains unavailable by design; it is not used as publication evidence.

- A live 2026-09-03 correction traced failed department creation to writing a department identifier into the user-only audit subject foreign key, and the empty/erroring user directory to ordering a projected LINQ record that EF Core could not translate. Department audit events now keep their department identifier only as safe metadata with no user subject, and the directory orders users before projection. A focused red/green application regression test passed; the correction was merged/pushed as `0c04600` and locally published with API readiness, UI entry, and the users SPA route returning HTTPS 200.

- The first administration release exposed a usability gap: create/edit forms opened as workspace tabs and no request-path regression test covered their submissions. The corrective slice now keeps those forms in source-page dialogs, restores a one-line footer date/time layout, uses Persian-calendar audit range selection, and records nullable direct-IP/coarse-device audit attribution through migration `20260903190243_AddAuditRequestAttribution`. Raw user-agent and forwarded headers are not retained or trusted. Request-path regression coverage and the rendered UI correction are complete; the earlier Release backend/frontend/browser verification and migration backup remain recorded, while the latest UI release is documented above. An authenticated administrator mutation smoke flow remains unrecorded.

- A live 2026-09-03 edit-user failure was traced to applying the detail lookup predicate to the projected record used by the user list; its nested department lookup and role-ID array could not be translated by EF Core for SQL Server. User details now use a SQL-translatable user/department join followed by a separate role-ID query. The focused integration regression test is red before the fix and green after it; Release build and typecheck passed. Elevated local IIS publication completed as release `20260903-224942`; API readiness and UI entry returned HTTPS 200, while the protected detail route correctly returned HTTP 401 without a session.

- A live 2026-09-03 save failure was traced to IIS WebDAV intercepting `PUT` requests before ASP.NET Core and returning `405.0` (`Allow: GET, HEAD, OPTIONS, TRACE`). The API `web.config` now removes the inherited `WebDAVModule` and `WebDAV` handler while retaining the ASP.NET Core `*` handler. A red/green Pester regression test covers the artifact configuration; after elevated publication as release `20260903-225950`, the same anonymous `PUT` correctly reached API authorization and returned `401`, while API readiness and UI entry returned HTTPS 200. No database migration was required.

- A repeated 2026-09-03 release defect was traced to invoking EF without `ASPNETCORE_ENVIRONMENT=Development`: EF inspected a different configured database and falsely reported the IIS development database current, leaving migration `20260903173032_AddSystemAdministrationAndAuditDashboard` unapplied. The migration was then applied to the IIS development database and its table/history entry were verified. The local IIS publisher now preflights the same development connection and refuses an application switch until its expected migration is present; the runbook requires the explicit environment setting. A user-confirmed local password sign-in succeeded after the correction. No connection values are recorded.
- The System Administrator user, department, audit-history, and operational-dashboard slice was merged into `main` as `454b960`, with complete audit workspace filters added in `116e24d`. Migration `20260903173032_AddSystemAdministrationAndAuditDashboard` adds the administration state. Only System Administrators can discover or call the protected `/api/v1/administration` surface. It supports safe paged user projections, fixed-role assignment, two-level department operations, temporary administrator password resets, target-session invalidation, and safe immutable audit projections. The Persian RTL workspace exposes four authorized targets, closable user forms, department forms, the five agreed 24-hour metrics, separate Persian date/time values, and seven-day, thirty-day, custom-range, event, actor, target-user, and result audit filters. Full Release backend verification passed 236 tests including 123 SQL-backed integration tests; frontend typecheck, formatting, IIS build, 30 component tests, and a mocked authenticated desktop/mobile dashboard browser flow passed. A verified local database backup was taken and the Release migration check reported the database already current. Elevated local publication completed as IIS release `20260903-214815`; HTTPS liveness, readiness, UI entry, and a refreshed internal SPA route each returned HTTP 200. No credential-bearing authenticated IIS smoke flow is recorded.
- The role-based dashboard foundation is integrated in `main` at commit `c199489`: the local development database migration created the fixed roles, `نرم افزار` and its `فناوری اطلاعات` child, and aligned the existing System Administrator with the Software department and Department Manager role. Release builds, 204 backend tests (including 115 SQL-backed integration tests), 28 frontend component/unit tests, and 7 browser flows passed. Local IIS release `20260903-184849` was published from committed `main` source `d4e15a5`; API liveness/readiness, the UI entry route, and a refreshed internal SPA route each returned HTTPS HTTP 200.
- The approved server-local timestamp and navigation correction was integrated into `main` as merge commit `98a457d`: migration `20260903154943_UseServerLocalMillisecondTimestamps` converts existing UTC offset values once to the SQL Server's local wall-clock values, renames all application timestamp columns and contracts without `Utc`, and stores them as `datetime2(3)`. The shell activates the fixed home tab without collapsing the persistent menu and bounds the temporary mobile drawer and backdrop above the status bar. Release backend verification passed 207 tests, including 116 SQL-backed integration tests; frontend typecheck, formatting, IIS build, 28 component/unit tests, and 7 browser flows passed. Local IIS release `20260903-193100` was built from committed `main` source `6cc64f7`; API liveness/readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200.
- A follow-up navigation-boundary correction on 2026-09-03 traced the remaining header gap and status-bar overlap to an obsolete 64px drawer offset while the themed header rendered at 58px, plus temporary Drawer paper styles that did not cross MUI's portal boundary. The header height is now a shared shell metric; both persistent and temporary Drawer paper use that metric, the status-bar metric, and explicit auto height. The mobile overlay uses the same bounds. Focused red/green browser regression coverage verifies exact desktop and mobile drawer edges; frontend typecheck, formatting, 28 component/unit tests, and 8 mocked browser flows passed. Local IIS release `20260903-201834` was published from committed `main` source `be85660`; API readiness, UI entry, refreshed SPA route, and authenticated desktop/mobile boundary checks passed over HTTPS.
- The local database migration for the server-local timestamp correction was initially omitted from the IIS release procedure, leaving the new API unable to query the old timestamp columns and making both local and Google sign-in fail. On 2026-09-03 a verified local database backup was taken, migration `20260903154943_UseServerLocalMillisecondTimestamps` was explicitly applied from the Release configuration, and safe local credential and Google-start smoke checks returned HTTP 403 and HTTP 302 respectively rather than server errors. The deployment runbook now requires a verified backup and Release-configuration migration check before an API switch.
- The local Google sign-in start route was diagnosed on 2026-09-03: its optional backchannel proxy had stopped, so OpenID metadata retrieval failed before the browser could be redirected to Google. The operator restarted the approved proxy, its availability was verified, and the API was restored to use it. The focused Google-configuration test set passed. Local IIS release `20260903-194345` returns HTTP 302 from the Google start route to `accounts.google.com`; API liveness/readiness and UI entry each return HTTPS HTTP 200. The proxy must remain available while the IIS application-pool identity requires it for Google's metadata and signing keys; signature validation remains enabled.
- If Google is temporarily unavailable while its authorization flow starts, the API now redirects to the local sign-in page instead of returning a technical JSON error. The sign-in page presents a specific Persian availability message while retaining local credential access; API and browser-flow regression tests cover both sides of this behavior. Local IIS release `20260903-195212` was built from committed `main` source `c074414`; API liveness/readiness and UI entry each returned HTTPS HTTP 200, and the available Google path returned HTTP 302 to Google.
- A user-provided manager-workforce dashboard screenshot is stored unchanged as `resources/images/references/manager-workforce-dashboard-reference.png`. It is the approved internal visual reference for compact dashboard composition and a role-filtered global command search that opens authorized results in workspace tabs; it does not approve or supply the depicted data, operations, or branding.
- System-administrator user-account/access management and company-department definition are implemented and merged into `main`, including protected routes, audited commands, safe audit-history queries, 24-hour metrics, and the approved Persian RTL management/dashboard UI. Custom roles, granular permissions, Google-link management, audit retention, exports, and alerts remain deferred. User-facing dates must use the Persian calendar, with date and time rendered separately when both appear.
- Resolve the configured external SMS endpoint/connectivity only if it recurs in a new local live sign-in attempt; the user confirmed that a full username/password-and-OTP sign-in succeeded on 2026-09-02.
- A local provisioning defect was corrected: Persian administrator profile text had been corrupted while being passed from the deployment helper to its child process, despite Unicode SQL Server columns. The helper and provisioner now declare UTF-8 at every process boundary. A safe database-wide text scan found exactly two affected values, both in the initial administrator's first/last-name fields, and none elsewhere. A parameterized corrective update restored the affected profile; a non-sensitive verification confirmed zero remaining corrupted profiles and one exact Unicode profile match. No private value is recorded in repository documentation.
- The private local deployment file is interpreted by named administrator fields rather than positional values; only username, password, first name, last name, and mobile are consumed. Extra labelled values are ignored. It is now reserved for an explicitly requested first-machine provisioning or repair, not ordinary publication.
- A local IIS publication helper now copies already-built API/UI artifacts, including the tracked API development configuration, to new versioned directories, switches the two IIS applications, and verifies readiness. It accepts no private-data-file argument and does not re-provision an administrator during a normal release.
- The local IIS UI was rebuilt with its `/EosDashboards/` asset base and `/EosDashboardsApi` API base, correcting the prior blank page and same-origin API routing failure. The user confirmed on 2026-09-03 that live username/password sign-in was tested successfully against the provisioned account. The subsequent configured SMS call timed out, so the resulting OTP was marked send-failed; no credential, code, phone number, or endpoint is recorded here.
- Elevated publication completed successfully on 2026-09-03. Local IIS release `20260903-110942` serves the current API and UI; a fresh isolated HTTPS browser visit loaded the current UI asset, reached the sign-in screen, and received the expected unauthenticated refresh rejection from the API. No local credential, OTP, or user session was used for that check.
- The approved EOS SVG is now the browser favicon rather than an active internal-workspace-tab ornament. Sign-in and password-recovery SMS OTP messages use distinct branded Persian templates; focused application tests assert the exact messages without logging a real OTP.
- The last selected appearance and palette are retained on the current device in addition to the per-user server preference. The root theme provider remains active across login and logout, so the sign-in page applies that saved theme before authentication can identify the user. On authentication, an existing device choice is authoritative and is synchronized to the server without first being overwritten by a stale/default server preference; focused component coverage protects this behavior.
- The sign-in page presents the approved EOS SVG transparently: only the desktop advertising panel has the brand mark, while the mobile form shows it above the card content at the right edge. Its recovery action has a padded hover target, and Chromium autofill states use the active theme's input surface, text, and caret colors instead of the browser's default fill.
- The user-provided geometric mesh is a login-brand-panel background only. It is stored as a project branding resource, uses a dark readability overlay, and does not appear behind workspace content.
- Login-brand-panel text uses fixed white contrast instead of a theme palette's `primary.contrastText`, so it remains readable above the dark background in every palette and appearance mode.
- Bootstrap refresh requests are coalesced in the browser, preventing React development strict-mode remounting from sending two concurrent refresh-token rotations for one page load. A mocked browser regression test asserts one refresh request restores the session. Secure-session manual validation remains restricted to the installed `https://localhost/EosDashboards/` application, not the HTTP Vite preview.
- OTP entry now distinguishes the five-minute code-validity window from its 60-second resend cooldown. After the cooldown, visitors can request a replacement challenge directly from the OTP view for sign-in or password recovery; the browser retains no password for this action and the prior challenge is superseded. A user-authorized local SMS test on 2026-09-03 was accepted by the configured provider without exposing any recipient, credential, or OTP value. Delivery to the handset remains an external provider/carrier concern.
- A local IIS SPA-refresh defect was diagnosed after release `20260903-113216`: a refreshed internal route returned IIS HTTP 403 because the UI's custom 404 executor targeted the parent-site root (`/`) rather than the UI application root. The published corrective artifact now targets `/EosDashboards/`, and the publisher includes a synthetic internal-route HTTP 200 check. The subsequent elevated publication and direct HTTPS checks of both the entry route and a synthetic internal route returned the application page with HTTP 200.
- Linked Google sign-in is available only for an active, pre-provisioned user explicitly linked to one Google identity. It uses the server-owned OpenID Connect authorization-code flow, retains the existing local-password/SMS-OTP route, and never creates users or permissions from Google data. The server configuration and a local IIS smoke flow were checked without recording private account, OAuth, token, or credential data; the activation runbook is `docs/operations/google-sign-in.md`.
- A release-source provenance incident was diagnosed on 2026-09-03: linked Google sign-in was published from an isolated feature worktree while the later UI work remained uncommitted in the main worktree, so IIS correctly served an older shell with only the new sign-in feature. The durable remedy is to integrate all related work into one committed release source, build both artifacts from that same worktree, inspect them before the IIS switch, and smoke-test the live HTTPS UI, authentication, session refresh, logout, and an internal SPA refresh afterwards. `docs/operations/iis-deployment.md` now contains the required release-source verification checklist; it must be followed for every local publication.
- The affected UI corrections and linked Google sign-in were integrated into one release source. Focused API integration tests, frontend component tests, browser-flow tests, and paired artifact inspection passed. Elevated publication completed as local IIS release `20260903-153200`; a fresh HTTPS visit confirmed the approved browser title, current sign-in UI, available Google action, and no relevant browser-console errors. The previously published older-shell release must not be treated as the current candidate.
- GitHub Push Protection blocked the merged local release after it detected the approved server-side local Google OAuth configuration. The repository owner explicitly approved the private-repository exception at action time, after which `main` was pushed. The release runbook records the durable rule: never mislabel a real credential, require current owner approval for a bypass, keep server settings out of frontend/logs/docs, and reassess rotation when repository exposure changes.
- The production deployment configuration is now documented as a complete tracked template at `backend/src/EosDashboards.Api/appsettings.Production.template.json`. The real `appsettings.Production.json` is server-only and ignored by Git; it must be created beside the published API files with the company's connection string, authentication keys, SMS settings, and optional Google settings, then loaded with the `Production` environment. This keeps the required configuration structure versioned without publishing production secrets.
- The English IIS installation guide was exported to `docs/operations/production-iis-installation-en.pdf` and linked from the repository landing page. The PDF uses the server-side `appsettings.Production.json` flow consistently, including loading the migration connection from that file without printing it.

## Current implementation note

On 2026-09-05, the job-description quality model was corrected so an unrelated
selected skill is allowed and does not create a defect. A required skill that
is still missing from a person's selected skills is also non-blocking: the
version remains `سالم` for workflow approval but persists `NeedsReview` and is
shown as `نیازمند بررسی` to the department manager and Chief Executive Officer.
Blocking quality findings remain separate and continue to produce
`منتظر رفع نقص`; both approval gates revalidate the blocking set. The manager
dashboard and list expose the review state, and the Chief Executive dashboard
has a read-only warning feed with the department, person, task, and missing
skill. Required-skill catalog changes revalidate active versions that use the
changed task.

Migration `20260905170000_AddJobDescriptionReviewWarning` was applied to the
local development database after a verified backup. It added the persisted
review flag and backfilled active non-approved versions without changing
approved or archived history. Release verification passed 55 Domain tests,
101 Application tests, 1 Architecture test, and 138 isolated SQL-backed
integration tests. The frontend suite passed 38 files/102 tests; typecheck,
IIS build, changed-file formatting, and lint completed with only the
repository's existing warnings. The first broad backend attempt correctly
stopped because the isolated SQL-test connection was missing; rerunning with
the documented isolated test database passed all SQL-backed tests.

The matching API/UI artifacts were published from `main` commit `72d860b` as
local IIS release `20260905-155956`. Post-publication checks returned HTTPS
HTTP 200 for API liveness, API readiness, the UI entry route, and the internal
job-description route. The authenticated manager page now shows the affected
records as `منتظر تأیید` and `سالم`; the database confirms migration
`20260905170000_AddJobDescriptionReviewWarning` is current. Documentation was
updated and the verified source was pushed to `origin/main` as commit
`00fea7d`. No company production deployment was performed.

A live manager check on 2026-09-05 found that repeated edits correctly retained
immutable history but the manager list projected every retained version as a
separate current row; this made one person appear five times after repeated
saves. The repository now groups manager-list results by job-description record
and returns only the latest version while preserving older versions for history
and comparison. The detail response now also returns catalog skill IDs with
their names, and the manager view renders the names instead of opaque IDs.
The new repository regression and focused manager-page regression both failed
before the correction and pass after it; the Release backend build and
frontend typecheck also pass. These source changes are verified locally but
were published from `main` commit `cf0045f` as local IIS release
`20260905-163632`. The post-publication authenticated browser check showed one
current row for پرهام جهانشاهی while the database retained all five immutable
versions, and the detail view displayed catalog names such as `سی شارپ` and
`دات نت فریمورک` instead of internal IDs.

A follow-up live check on 2026-09-05 found that the Human Resources review
list showed a healthy manager-approved version as `ناقص`, while the manager
list showed `سالم`. The root cause was that the Human Resources repository
queries calculated the derived quality status without loading the version's
tasks, skills, or unresolved collections; the empty in-memory collections made
every listed version appear incomplete. Both Human Resources list queries now
reuse the full version-detail includes. The regression test failed before the
correction and passes after it, as do all 11 repository integration tests and
the Release backend build. This source correction has not yet been published
to IIS.

## Next agreed step

Publish the Human Resources quality-status correction to local IIS only after
the user explicitly invokes `نهایی کن`. Do not deploy to company production
servers.

## Blockers

- The configured local SMS service endpoint timed out during one authorized live sign-in attempt. Login cannot complete until its endpoint, network reachability, or SOAP service contract is corrected. Do not repeat live OTP sends until that is resolved.

## Immediate unresolved questions

- Which business dashboards and metrics are required first beyond the approved
  System Administrator dashboard?
- Which managers or roles will use each dashboard?
- What source systems will supply dashboard data?
- Is the organization's LDAP service separate from Active Directory, and which supported identity infrastructure is available?
- What are the approved internal hostnames, certificates, browser versions, retention periods, recovery objectives, and monitoring tools?
- What organizational retention, ownership, and non-administrator-access policy
  applies to audit records?
