# Current Project State

**Last updated:** 2026-09-03

## Phase

Initial authentication and application-shell implementation.

## Completed

- The empty GitHub repository was cloned locally.
- The repository-based project-memory design was approved.
- The implementation plan for the memory structure was prepared.
- The canonical project-memory structure and root agent instructions were created and verified.
- Project-wide development, UI, security, data, testing, deployment, and operational standards were approved.
- The initial intranet authentication experience and the deferred external-access discovery boundary were approved.
- Material UI, RTL-first Persian presentation, theme behavior, the amber default palette with five further approved choices, and Vazirmatn were selected.
- The approved standards were consolidated into canonical English and formal Persian documents.
- The repository-level shared resource hierarchy was created.
- The first implementation slice was approved: separate backend/frontend foundations, pre-provisioned system administrator, local username/password sign-in, mandatory SMS OTP, session management, and the initial SPA shell.
- Company branding, footer version/date/time, and closable internal workspace tabs were approved.
- The initial authentication and tabbed-shell design was reviewed and approved.
- A task-by-task, test-driven implementation plan was prepared.
- The user selected inline plan execution with review checkpoints.
- .NET SDK 10.0.400 and Node.js 24.19.0 were installed and verified.
- The isolated `feature/initial-authentication-shell` worktree was created.
- The independently buildable backend solution and React frontend foundation were scaffolded and verified.
- The Domain authentication, session, role, preference, and audit state model was implemented and verified.
- The Application authentication contracts and use cases were implemented and verified with correlation-aware audit attribution, non-cancelable OTP security commits, and access tokens capped by absolute session expiry.
- EF Core SQL Server persistence, explicit repositories, guarded database integration tests, the initial identity migration, and its reviewed idempotent deployment script were implemented and review-hardened with tracked provisioning mutations plus live uniqueness and rowversion-concurrency coverage.
- Infrastructure authentication security primitives were implemented, review-hardened, and verified: secure OTP/opaque-token generation, keyed HMAC-SHA256 hashing, restart-persistent purpose-isolated Data Protection mobile encryption, strict JWT issuer/audience/signature/HS256/lifetime validation with explicit expiry, typed startup-validated security options, and dependency injection. Pending a formal mask-format decision, the implementation conservatively exposes only the final four mobile digits (`*******6789`).
- The replaceable company SOAP SMS adapter was implemented and verified: typed startup-validated HTTPS endpoint/timeout options, one named `HttpClient`, SOAP 1.1 request creation with XML-safe message/mobile serialization, a configured deadline across headers and asynchronously read body, a fully buffered 64 KiB response limit before DTD-prohibited EOF-complete parsing with exactly one result, safe failure mapping, caller-cancellation propagation, and no automatic retry.
- The deployment-only System Administrator provisioner was implemented and verified: normalized idempotent user/role creation and profile updates, exact role-code validation, protected mobile storage with masked-only output, cross-process serialized transactional generated-ID/role/audit persistence, safe interactive confirmation, and composition independent of SMS configuration.
- The user required a cost-conscious development workflow: focused essential tests during implementation, broad verification only at meaningful checkpoints, and no subagents or repeated independent review loops without explicit approval or exceptional documented risk.
- The user explicitly approved tracking local development server settings, including SQL credentials, service endpoints, and API security keys, in this private repository's API/IIS configuration. Public frontend endpoint settings may also be tracked; server credentials and private keys remain server-side because frontend build values are browser-visible. Use the established `D:\Workspaces\ChatGpt\Private Data For AI Projects\EosDashboards` directory as a fallback without repeatedly requesting its values. Values remain excluded from documentation and tool output; production values and personal data remain outside source control.
- The available SQL Server database and IIS sites are development-only resources on the user's machine. Task 13 targets them for local deployment and smoke testing; production deployment to company servers remains a later, separate activity.
- Secure API endpoints expose local username/password challenge creation, purpose-isolated OTP verification, password recovery and signed-in password change, JWT-protected current-user access, refresh rotation, logout, current-user preferences, safe problem details, rate limiting, exact-origin CORS, OpenAPI, and live/ready health checks.
- The React SPA has a locally hosted Vazirmatn font, Persian RTL Material UI theme, polished local-credential sign-in, recovery and password-change views, in-memory access-token client, fixed application shell, home tab, serializable closable-tab workspace, Persian status clock, and server-synchronized appearance/sidebar preferences.
- A 2026-09-03 UI correction replaced the raw session-check text with a branded loading state, removed the forced light sign-in theme, corrected dark-mode contrast tokens and primary-button hover text, made sidebar closure functional, and moved the status bar to the full application width. A repeated development sign-in failure was traced to Vite returning its own 404 for `/api/*` because it had no API proxy; the generic credential/SMS error then concealed that routing defect. Vite now proxies `/api/*` to the local IIS API while presenting the approved local UI origin. A browser regression test requires the API's `refresh_rejected` response rather than Vite's 404, so development execution cannot silently lose its API route again. Regression coverage also verifies the loading state, saved dark appearance, full-width status bar, sidebar interactions, and primary-button hover contrast with a simulated credential/OTP flow.
- A follow-up rendered-UI correction on 2026-09-03 found that a failed preference write rolled back the user's theme and sidebar choice, making both controls appear broken. Local choices now remain applied and are retained per user even when server synchronization fails, without interrupting the workspace with a repeated user-visible notice. RTL Drawer placement now opens beside the right-side menu control, internal workspace tabs remain neutral and unbranded, the browser tab title is `داشبوردهای علم و صنعت`, and the home content fills and aligns with the workspace at every width. A deployed-IIS browser check also found that the workspace tab provider dropped the `/EosDashboards/` application base from the browser URL; it now preserves that base for all tab URLs. Component and mocked-network browser tests cover the fallback, title, alignment, Drawer edge, and IIS base-path behavior.
- The approved UI quality correction on 2026-09-03 made the first appearance-toggle click compare against the resolved system appearance, so it always changes the rendered theme. It added six persisted palettes (forest green, navy, turquoise, plum, amber, and burgundy), with amber as the default and a deep restrained brown in dark mode. On phones the navigation is now a temporary overlay and never reduces the workspace width; its modal starts below the fixed header and ends above the status bar, has an in-panel close action, and can also be dismissed by touching outside it. OTP phone numbers and password entry are explicitly left-to-right inside the Persian RTL interface, and the recovery action has deliberate spacing. Focused component tests, backend preference validation, mocked authenticated-browser tests, and desktop/mobile browser checks cover these behaviors.
- Amber became the default palette on 2026-09-03 for users without an existing server or local palette preference. Existing saved palette choices are preserved. IIS release `20260903-115548` serves this UI artifact successfully over HTTPS.
- A 2026-09-03 browser investigation confirmed that an HTTP Vite preview cannot be used to validate the secure `__Host-` refresh-session lifecycle. Manual local credential, OTP, refresh, logout, and server-preference validation must use the installed `https://localhost/EosDashboards/` IIS application; the Vite preview remains suitable for frontend development and mocked browser tests. The refresh cookie policy remains Secure and is not weakened for development.
- Fresh pre-integration verification passed on 2026-09-03: the Release backend suite completed 172 tests, including 102 SQL-backed integration tests; frontend lint, typecheck, formatting, build, 10 component/unit tests, and one mocked-network browser flow completed successfully. Separate API/UI publish artifact inspection previously found no source maps or detected embedded secrets.
- The approved manager-facing workforce-operations UI alignment was integrated into `main` on 2026-09-03 as merge commit `18ff610`: dark/teal is now the default; teal, indigo, emerald, amber, and rose are the supported persisted accents; legacy stored palette values safely render as teal; the theme supplies the approved dark/light tokens, compact geometry, flat panels, short motion, and gold tab indicator. The existing persistent collapsible navigation behavior remains unchanged. The home tab is an honest operational no-data state; no dashboard data, workflows, or simulated status was introduced. Fresh post-merge verification passed: 200 Release backend tests, including 115 SQL-backed integration tests; frontend typecheck, formatting, production build, 25 component/unit tests, and 7 browser flows. Frontend lint completed with existing React warnings only; desktop and mobile browser checks confirmed the dark shell, palette toggle, tab, and mobile navigation without relevant console errors. It was published to local IIS release `20260903-171004` from `main` commit `15565b2`; API readiness, UI entry, and an internal SPA refresh route returned HTTPS HTTP 200, and a fresh browser visit showed the current sign-in screen with no console errors. No authenticated smoke flow was run because no user credential was supplied.
- A local browser-test-port collision was resolved without touching an unrelated Python service: Playwright now accepts `EOS_PLAYWRIGHT_PORT` and was verified on port 4174. This prevents the test suite from accidentally reusing content served by a process outside the project.
- Local development deployment artifacts were produced in a versioned temporary directory. Both API and UI contain `web.config`; the reviewed initial idempotent SQL migration script remains available.
- The initial authentication and application-shell implementation was merged into `main` and published to the configured GitHub remote on 2026-09-03 as merge commit `d51fe35`. The external SMS connectivity blocker remains explicit and does not invalidate the user-confirmed live username/password-and-OTP sign-in completed on 2026-09-02.
- The approved EOS SVG was received on 2026-09-02, stored under `resources/branding/eos.svg` without modification, and verified against its SHA-256 record.
- The local IIS deployment uses separate `Default Web Site` applications: UI `/EosDashboards` in `EosDashboardsUiPool` and API `/EosDashboardsApi` in `EosDashboardsApiPool`, both under `C:\inetpub\wwwroot\EosDashboards\` with immutable versioned releases. Both pools use No Managed Code; the current local UI and API readiness endpoints return HTTPS HTTP 200.
- IIS URL Rewrite is not installed locally. The UI SPA fallback uses IIS custom 404 execution, avoiding that unavailable module. API Anonymous Authentication is enabled and Windows Authentication is disabled. The API also disables IIS automatic authentication in-process so the retired Negotiate handler cannot be injected during local credential sign-in.
- The development SQL connection, SMS endpoint, authentication keys, local IIS origin, and API-only key-ring path are tracked in the API development configuration under decision 0006. The persistent key ring remains outside the web root. The deprecated private-data helper is limited to explicitly requested first-administrator provisioning or repair. The local development migration completed and one initial System Administrator user, role assignment, and provisioning audit record were created. No personal or production value is stored in this repository.
- The authentication documentation was aligned on 2026-09-03: the only phase-1 sign-in flow is pre-provisioned username/password followed by mandatory SMS OTP. Legacy integrated-directory sign-in wording was removed from current specifications, decisions, and operations guidance.
- A follow-up administration UI correction on 2026-09-03 resolved three rendered defects: the create-user dialog spinner was caused by treating React Query's intentionally disabled user-details query as pending; the create/edit form surfaces now fill their dialog width; and every manager-facing accent panel uses the shared `eos-accent-card` hover rule to turn its top line gold. Red/green component tests cover the create form, dialog width, and theme rule. Release `20260903-231951` was built from committed source `46326ea` and verified in Chrome at desktop and phone sizes: the create-user and create-department dialogs rendered their fields, desktop form/card gaps were limited to the surrounding 1px border, and a hovered dashboard card computed the gold `#E0A13A` top line. API readiness, UI entry, and a refreshed department route returned HTTPS HTTP 200; no relevant browser-console messages were present.
- The redundant user-account-name field was removed on 2026-09-03 after confirming that local sign-in uses the username and Google sign-in uses its linked identity. The field is gone from user forms, administration request/response contracts, authenticated-user responses, the domain model, and the deployment provisioner; migration `20260903200248_RemoveUserAccountName` drops the legacy SQL column and index. A verified backup was taken and this migration was applied to the local development database, with the column absence and migration-history entry confirmed. Publishing the matching IIS API/UI release remains pending because the current shell cannot run the publisher with elevated PowerShell permissions.

## In progress

- A live 2026-09-03 correction traced failed department creation to writing a department identifier into the user-only audit subject foreign key, and the empty/erroring user directory to ordering a projected LINQ record that EF Core could not translate. Department audit events now keep their department identifier only as safe metadata with no user subject, and the directory orders users before projection. A focused red/green application regression test passed; the correction was merged/pushed as `0c04600` and locally published with API readiness, UI entry, and the users SPA route returning HTTPS 200.

- The first administration release exposed a usability gap: create/edit forms opened as workspace tabs and no request-path regression test covered their submissions. The corrective slice now keeps those forms in source-page dialogs, restores a one-line footer date/time layout, uses Persian-calendar audit range selection, and records nullable direct-IP/coarse-device audit attribution through migration `20260903190243_AddAuditRequestAttribution`. Raw user-agent and forwarded headers are not retained or trusted. Request-path regression coverage and the rendered UI correction are complete; the earlier Release backend/frontend/browser verification and migration backup remain recorded, while the latest UI release is documented above. An authenticated administrator mutation smoke flow remains unrecorded.

- A live 2026-09-03 edit-user failure was traced to applying the detail lookup predicate to the projected record used by the user list; its nested department lookup and role-ID array could not be translated by EF Core for SQL Server. User details now use a SQL-translatable user/department join followed by a separate role-ID query. The focused integration regression test is red before the fix and green after it; Release build and typecheck passed. Elevated local IIS publication completed as release `20260903-224942`; API readiness and UI entry returned HTTPS 200, while the protected detail route correctly returned HTTP 401 without a session.

- A live 2026-09-03 save failure was traced to IIS WebDAV intercepting `PUT` requests before ASP.NET Core and returning `405.0` (`Allow: GET, HEAD, OPTIONS, TRACE`). The API `web.config` now removes the inherited `WebDAVModule` and `WebDAV` handler while retaining the ASP.NET Core `*` handler. A red/green Pester regression test covers the artifact configuration; after elevated publication as release `20260903-225950`, the same anonymous `PUT` correctly reached API authorization and returned `401`, while API readiness and UI entry returned HTTPS 200. No database migration was required.

- A repeated 2026-09-03 release defect was traced to invoking EF without `ASPNETCORE_ENVIRONMENT=Development`: EF inspected a different configured database and falsely reported the IIS development database current, leaving migration `20260903173032_AddSystemAdministrationAndAuditDashboard` unapplied. The migration was then applied to the IIS development database and its table/history entry were verified. The local IIS publisher now preflights the same development connection and refuses an application switch until its expected migration is present; the runbook requires the explicit environment setting. A user-confirmed local password sign-in succeeded after the correction. No connection values are recorded.
- The System Administrator user, department, audit-history, and operational-dashboard slice was merged into `main` as `454b960`, with complete audit workspace filters added in `116e24d`. Migration `20260903173032_AddSystemAdministrationAndAuditDashboard` adds the administration state. Only System Administrators can discover or call the protected `/api/v1/administration` surface. It supports safe paged user projections, fixed-role assignment, two-level department operations, temporary administrator password resets, target-session invalidation, and safe immutable audit projections. The Persian RTL workspace exposes four authorized targets, closable user forms, department forms, the five agreed 24-hour metrics, separate Persian date/time values, and seven-day, thirty-day, custom-range, event, actor, target-user, and result audit filters. Full Release backend verification passed 236 tests including 123 SQL-backed integration tests; frontend typecheck, formatting, IIS build, 30 component tests, and a mocked authenticated desktop/mobile dashboard browser flow passed. A verified local database backup was taken and the Release migration check reported the database already current. Elevated local publication completed as IIS release `20260903-214815`; HTTPS liveness, readiness, UI entry, and a refreshed internal SPA route each returned HTTP 200. No credential-bearing authenticated IIS smoke flow is recorded.
- The role-based dashboard foundation is integrated in `main` at commit `c199489`: the local development database migration created the fixed roles, `نرم افزار` and its `فناوری اطلاعات` child, and aligned the existing System Administrator with the Software department and Department Manager role. Release builds, 204 backend tests (including 115 SQL-backed integration tests), 28 frontend component/unit tests, and 7 browser flows passed. Local IIS release `20260903-184849` was published from committed `main` source `d4e15a5`; API liveness/readiness, the UI entry route, and a refreshed internal SPA route each returned HTTPS HTTP 200.
- The approved server-local timestamp and navigation correction was integrated into `main` as merge commit `98a457d`: migration `20260903154943_UseServerLocalMillisecondTimestamps` converts existing UTC offset values once to the SQL Server's local wall-clock values, renames all application timestamp columns and contracts without `Utc`, and stores them as `datetime2(3)`. The shell activates the fixed home tab without collapsing the persistent menu and bounds the temporary mobile drawer and backdrop above the status bar. Release backend verification passed 207 tests, including 116 SQL-backed integration tests; frontend typecheck, formatting, IIS build, 28 component/unit tests, and 7 browser flows passed. Local IIS release `20260903-193100` was built from committed `main` source `6cc64f7`; API liveness/readiness, UI entry, and the refreshed internal SPA route each returned HTTPS HTTP 200.
- A follow-up navigation-boundary correction on 2026-09-03 traced the remaining header gap and status-bar overlap to an obsolete 64px drawer offset while the themed header rendered at 58px, plus temporary Drawer paper styles that did not cross MUI's portal boundary. The header height is now a shared shell metric; both persistent and temporary Drawer paper use that metric, the status-bar metric, and explicit auto height. The mobile overlay uses the same bounds. Focused red/green browser regression coverage verifies exact desktop and mobile drawer edges; frontend typecheck, formatting, 28 component/unit tests, and 8 mocked browser flows passed. Local IIS release `20260903-201834` was published from committed `main` source `be85660`; API readiness, UI entry, refreshed SPA route, and authenticated desktop/mobile boundary checks passed over HTTPS.
- The local database migration for the server-local timestamp correction was initially omitted from the IIS release procedure, leaving the new API unable to query the old timestamp columns and making both local and Google sign-in fail. On 2026-09-03 a verified local database backup was taken, migration `20260903154943_UseServerLocalMillisecondTimestamps` was explicitly applied from the Release configuration, and safe local credential and Google-start smoke checks returned HTTP 403 and HTTP 302 respectively rather than server errors. The deployment runbook now requires a verified backup and Release-configuration migration check before an API switch.
- The local Google sign-in start route was diagnosed on 2026-09-03: its optional backchannel proxy had stopped, so OpenID metadata retrieval failed before the browser could be redirected to Google. The operator restarted the approved proxy, its availability was verified, and the API was restored to use it. The focused Google-configuration test set passed. Local IIS release `20260903-194345` returns HTTP 302 from the Google start route to `accounts.google.com`; API liveness/readiness and UI entry each return HTTPS HTTP 200. The proxy must remain available while the IIS application-pool identity requires it for Google's metadata and signing keys; signature validation remains enabled.
- If Google is temporarily unavailable while its authorization flow starts, the API now redirects to the local sign-in page instead of returning a technical JSON error. The sign-in page presents a specific Persian availability message while retaining local credential access; API and browser-flow regression tests cover both sides of this behavior. Local IIS release `20260903-195212` was built from committed `main` source `c074414`; API liveness/readiness and UI entry each returned HTTPS HTTP 200, and the available Google path returned HTTP 302 to Google.
- A user-provided manager-workforce dashboard screenshot is stored unchanged as `resources/images/references/manager-workforce-dashboard-reference.png`. It is the approved internal visual reference for compact dashboard composition and a role-filtered global command search that opens authorized results in workspace tabs; it does not approve or supply the depicted data, operations, or branding.
- System-administrator user-account/access management and company-department definition are approved scope. Implementation is underway in the isolated `codex/system-administration-and-audit-dashboard` branch: mutable account/department concurrency, temporary-password enforcement, audited user and department commands, safe audit-history queries, and 24-hour system metrics are implemented and focused-tested. Protected API routes and the approved Persian RTL management/dashboard UI remain next. Custom roles, granular permissions, Google-link management, audit retention, exports, and alerts remain deferred. User-facing dates must use the Persian calendar, with date and time rendered separately when both appear.
- Resolve the configured external SMS endpoint/connectivity only if it recurs in a new local live sign-in attempt; the user confirmed that a full username/password-and-OTP sign-in succeeded on 2026-09-02.
- A local provisioning defect was corrected: Persian administrator profile text had been corrupted while being passed from the deployment helper to its child process, despite Unicode SQL Server columns. The helper and provisioner now declare UTF-8 at every process boundary. A safe database-wide text scan found exactly two affected values, both in the initial administrator's first/last-name fields, and none elsewhere. A parameterized corrective update restored the affected profile; a non-sensitive verification confirmed zero remaining corrupted profiles and one exact Unicode profile match. No private value is recorded in repository documentation.
- The private local deployment file is interpreted by named administrator fields rather than positional values; only username, password, first name, last name, and mobile are consumed. Extra labelled values are ignored. It is now reserved for an explicitly requested first-machine provisioning or repair, not ordinary publication.
- A local IIS publication helper now copies already-built API/UI artifacts, including the tracked API development configuration, to new versioned directories, switches the two IIS applications, and verifies readiness. It accepts no private-data-file argument and does not re-provision an administrator during a normal release.
- The local IIS UI was rebuilt with its `/EosDashboards/` asset base and `/EosDashboardsApi` API base, correcting the prior blank page and same-origin API routing failure. The user confirmed on 2026-09-03 that live username/password sign-in was tested successfully against the provisioned account. The subsequent configured SMS call timed out, so the resulting OTP was marked send-failed; no credential, code, phone number, or endpoint is recorded here.
- Elevated publication completed successfully on 2026-09-03. Local IIS release `20260903-110942` serves the current API and UI; a fresh isolated HTTPS browser visit loaded the current UI asset, reached the sign-in screen, and received the expected unauthenticated refresh rejection from the API. No local credential, OTP, or user session was used for that check.
- The approved EOS SVG is now the browser favicon rather than an active internal-workspace-tab ornament. Sign-in and password-recovery SMS OTP messages use distinct branded Persian templates; focused application tests assert the exact messages without logging a real OTP.
- The last selected appearance and palette are retained on the current device in addition to the per-user server preference. The root theme provider remains active across login and logout, so the sign-in page applies that saved theme before authentication can identify the user. On authentication, an existing device choice is authoritative and is synchronized to the server without first being overwritten by a stale/default server preference; focused component coverage protects this behavior.
- The sign-in page keeps the approved EOS SVG transparent rather than enclosing it in an opaque tile. Its recovery action has a padded hover target, and Chromium autofill states use the active theme's input surface, text, and caret colors instead of the browser's default fill.
- The user-provided geometric mesh is a login-brand-panel background only. It is stored as a project branding resource, uses a dark readability overlay, and does not appear behind workspace content.
- Login-brand-panel text uses fixed white contrast instead of a theme palette's `primary.contrastText`, so it remains readable above the dark background in every palette and appearance mode.
- Bootstrap refresh requests are coalesced in the browser, preventing React development strict-mode remounting from sending two concurrent refresh-token rotations for one page load. A mocked browser regression test asserts one refresh request restores the session. Secure-session manual validation remains restricted to the installed `https://localhost/EosDashboards/` application, not the HTTP Vite preview.
- OTP entry now distinguishes the five-minute code-validity window from its 60-second resend cooldown. After the cooldown, visitors can request a replacement challenge directly from the OTP view for sign-in or password recovery; the browser retains no password for this action and the prior challenge is superseded. A user-authorized local SMS test on 2026-09-03 was accepted by the configured provider without exposing any recipient, credential, or OTP value. Delivery to the handset remains an external provider/carrier concern.
- A local IIS SPA-refresh defect was diagnosed after release `20260903-113216`: a refreshed internal route returned IIS HTTP 403 because the UI's custom 404 executor targeted the parent-site root (`/`) rather than the UI application root. The published corrective artifact now targets `/EosDashboards/`, and the publisher includes a synthetic internal-route HTTP 200 check. The subsequent elevated publication and direct HTTPS checks of both the entry route and a synthetic internal route returned the application page with HTTP 200.
- Linked Google sign-in is available only for an active, pre-provisioned user explicitly linked to one Google identity. It uses the server-owned OpenID Connect authorization-code flow, retains the existing local-password/SMS-OTP route, and never creates users or permissions from Google data. The server configuration and a local IIS smoke flow were checked without recording private account, OAuth, token, or credential data; the activation runbook is `docs/operations/google-sign-in.md`.
- A release-source provenance incident was diagnosed on 2026-09-03: linked Google sign-in was published from an isolated feature worktree while the later UI work remained uncommitted in the main worktree, so IIS correctly served an older shell with only the new sign-in feature. The durable remedy is to integrate all related work into one committed release source, build both artifacts from that same worktree, inspect them before the IIS switch, and smoke-test the live HTTPS UI, authentication, session refresh, logout, and an internal SPA refresh afterwards. `docs/operations/iis-deployment.md` now contains the required release-source verification checklist; it must be followed for every local publication.
- The affected UI corrections and linked Google sign-in were integrated into one release source. Focused API integration tests, frontend component tests, browser-flow tests, and paired artifact inspection passed. Elevated publication completed as local IIS release `20260903-153200`; a fresh HTTPS visit confirmed the approved browser title, current sign-in UI, available Google action, and no relevant browser-console errors. The previously published older-shell release must not be treated as the current candidate.
- GitHub Push Protection blocked the merged local release after it detected the approved server-side local Google OAuth configuration. The repository owner explicitly approved the private-repository exception at action time, after which `main` was pushed. The release runbook records the durable rule: never mislabel a real credential, require current owner approval for a bypass, keep server settings out of frontend/logs/docs, and reassess rotation when repository exposure changes.

## Next agreed step

Continue product discovery for the first business dashboard and its approved
metrics, data source, users, and authorization. Do not deploy to company
production servers in this slice.

## Blockers

- The configured local SMS service endpoint timed out during one authorized live sign-in attempt. Login cannot complete until its endpoint, network reachability, or SOAP service contract is corrected. Do not repeat live OTP sends until that is resolved.

## Immediate unresolved questions

- Which business dashboards and metrics are required first beyond the approved
  System Administrator dashboard?
- Which managers or roles will use each dashboard?
- What source systems will supply dashboard data?
- Is the organization's LDAP service separate from Active Directory, and which supported identity infrastructure is available?
- What are the approved internal hostnames, certificates, browser versions, retention periods, recovery objectives, and monitoring tools?
- What organizational retention, ownership, and non-administrator-access policy
  applies to audit records?
